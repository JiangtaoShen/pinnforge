# b00 — control: LDC baseline measured as-is (rRMSE 0.9444)

## Thrust & rationale
Setup control node, not a subagent product. `b00_v01.py` is
`task/baseline.py` verbatim — the vanilla plain-MLP PINN paradigm (single
tanh MLP, fixed-LR Adam, soft BC penalties, strict-interior collocation)
for the 2-D steady lid-driven cavity at Re=3200 on the unit square.
Measured as-is at the task's 150 s wall budget to anchor the number every
later block must beat.

## Evals
| # | file | change vs parent | rRMSE | steps | note |
|---|------|------------------|-------|-------|------|
| 1 | b00_v01.py | baseline as-is (= task/baseline.py) | 0.9444 | 133357 | seed 0, 145.5 s, GPU 0 |

## Findings
Baseline anchor for LDC Re=3200: rRMSE **0.9444** at 150 s / 133.4k Adam
steps (seed 0). The vanilla plain-MLP PINN barely departs from the trivial
field — high-Re lid-driven cavity is a stiff problem (steep boundary
layers, corner singularities, and the residual/BC gradient imbalance kb1
026/050 describe). Wide open for the documented levers: loss weighting,
causal/curriculum training, adaptive sampling, second-order optimizers, and
Scale-PINN sequential correction (kb1 126, the LDC/Scale-PINN line). Number
to beat: **0.9444**.
